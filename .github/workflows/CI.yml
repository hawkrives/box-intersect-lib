# This file is autogenerated by maturin v1.11.5
# To update, run
#
#    maturin generate-ci github --pytest -m crates/box-intersect-lib-py/Cargo.toml --output .github/workflows/CI.yml
#
name: CI

on:
  push:
    branches:
      - main
      - master
    tags:
      - '*'
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  linux:
    runs-on: ${{ matrix.platform.runner }}
    strategy:
      matrix:
        platform:
          - runner: ubuntu-22.04
            target: x86_64
          - runner: ubuntu-22.04
            target: x86
          - runner: ubuntu-22.04
            target: aarch64
          - runner: ubuntu-22.04
            target: armv7
          - runner: ubuntu-22.04
            target: s390x
          - runner: ubuntu-22.04
            target: ppc64le
    steps:
      - uses: actions/checkout@v6
      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          working-directory: crates/box-intersect-lib-py
          python-version: '3'
      - name: Set up Python
        run: uv python install
      - name: Build wheels
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.platform.target }}
          args: --release --out dist --find-interpreter --manifest-path crates/box-intersect-lib-py/Cargo.toml
          sccache: ${{ !startsWith(github.ref, 'refs/tags/') }}
          manylinux: auto
      - name: Upload wheels
        uses: actions/upload-artifact@v5
        with:
          name: wheels-linux-${{ matrix.platform.target }}
          path: dist
      - name: pytest
        if: ${{ startsWith(matrix.platform.target, 'x86_64') }}
        shell: bash
        run: |
          set -ex
          cd crates/box-intersect-lib-py
          uv sync --no-install-project --no-build
          ls -l ../../dist
          cd ../..
          uv pip install box_intersect_lib_py --find-links dist --force-reinstall --no-index --python crates/box-intersect-lib-py/.venv/bin/python
          cd crates/box-intersect-lib-py
          uv run --no-sync pytest
      - name: pytest
        if: ${{ !startsWith(matrix.platform.target, 'x86') && matrix.platform.target != 'ppc64' }}
        uses: uraimo/run-on-arch-action@v2
        with:
          arch: ${{ matrix.platform.target }}
          distro: ubuntu22.04
          githubToken: ${{ github.token }}
          install: |
            apt-get update
            apt-get install -y --no-install-recommends python3 python3-pip curl ca-certificates
            pip3 install -U pip
            curl -LsSf https://astral.sh/uv/install.sh | sh
          run: |
            set -ex
            export PATH="/root/.local/bin:$PATH"
            cd crates/box-intersect-lib-py
            uv sync --no-install-project --no-build
            ls -l ../../dist
            cd ../..
            uv pip install box_intersect_lib_py --find-links dist --force-reinstall --no-index --python crates/box-intersect-lib-py/.venv/bin/python
            cd crates/box-intersect-lib-py
            uv run --no-sync pytest

  musllinux:
    runs-on: ${{ matrix.platform.runner }}
    strategy:
      matrix:
        platform:
          - runner: ubuntu-22.04
            target: x86_64
          - runner: ubuntu-22.04
            target: x86
          - runner: ubuntu-22.04
            target: aarch64
          - runner: ubuntu-22.04
            target: armv7
    steps:
      - uses: actions/checkout@v6
      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          working-directory: crates/box-intersect-lib-py
          python-version: '3'
      - name: Set up Python
        run: uv python install
      - name: Build wheels
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.platform.target }}
          args: --release --out dist --find-interpreter --manifest-path crates/box-intersect-lib-py/Cargo.toml
          sccache: ${{ !startsWith(github.ref, 'refs/tags/') }}
          manylinux: musllinux_1_2
      - name: Upload wheels
        uses: actions/upload-artifact@v5
        with:
          name: wheels-musllinux-${{ matrix.platform.target }}
          path: dist
      - name: pytest
        if: ${{ startsWith(matrix.platform.target, 'x86_64') }}
        uses: addnab/docker-run-action@v3
        with:
          image: alpine:latest
          options: -v ${{ github.workspace }}:/io -w /io
          run: |
            set -ex
            apk add py3-pip py3-virtualenv curl ca-certificates
            curl -LsSf https://astral.sh/uv/install.sh | sh
            export PATH="/root/.local/bin:$PATH"
            cd crates/box-intersect-lib-py
            uv sync --no-install-project --no-build
            ls -l ../../dist
            cd ../..
            uv pip install box_intersect_lib_py --find-links dist --force-reinstall --no-index --python crates/box-intersect-lib-py/.venv/bin/python
            cd crates/box-intersect-lib-py
            uv run --no-sync pytest
      - name: pytest
        if: ${{ !startsWith(matrix.platform.target, 'x86') }}
        uses: uraimo/run-on-arch-action@v2
        with:
          arch: ${{ matrix.platform.target }}
          distro: alpine_latest
          githubToken: ${{ github.token }}
          install: |
            apk add py3-virtualenv curl ca-certificates
            curl -LsSf https://astral.sh/uv/install.sh | sh
          run: |
            set -ex
            export PATH="/root/.local/bin:$PATH"
            cd crates/box-intersect-lib-py
            uv sync --no-install-project --no-build
            ls -l ../../dist
            cd ../..
            uv pip install box_intersect_lib_py --find-links dist --force-reinstall --no-index --python crates/box-intersect-lib-py/.venv/bin/python
            cd crates/box-intersect-lib-py
            uv run --no-sync pytest

  windows:
    runs-on: ${{ matrix.platform.runner }}
    strategy:
      matrix:
        platform:
          - runner: windows-latest
            target: x64
            python_arch: x86_64
          - runner: windows-latest
            target: x86
            python_arch: i686
          - runner: windows-11-arm
            target: aarch64
            python_arch: aarch64
    steps:
      - uses: actions/checkout@v6
      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          working-directory: crates/box-intersect-lib-py
          python-version: 3.13-${{ matrix.platform.python_arch }}
      - name: Set up Python
        run: uv python install
      - name: Add Python to PATH
        shell: pwsh
        run: echo "$env:USERPROFILE\.local\bin" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append
      - name: Build wheels
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.platform.target }}
          args: --release --out dist --find-interpreter --manifest-path crates/box-intersect-lib-py/Cargo.toml
          sccache: ${{ !startsWith(github.ref, 'refs/tags/') }}
      - name: Upload wheels
        uses: actions/upload-artifact@v5
        with:
          name: wheels-windows-${{ matrix.platform.target }}
          path: dist
      - name: pytest
        shell: bash
        run: |
          set -ex
          cd crates/box-intersect-lib-py
          uv sync --no-install-project --no-build
          ls -l ../../dist
          cd ../..
          uv pip install box_intersect_lib_py --find-links dist --force-reinstall --no-index --python crates/box-intersect-lib-py/.venv/Scripts/python.exe
          cd crates/box-intersect-lib-py
          uv run --no-sync pytest

  macos:
    runs-on: ${{ matrix.platform.runner }}
    strategy:
      matrix:
        platform:
          - runner: macos-15-intel
            target: x86_64
          - runner: macos-latest
            target: aarch64
    steps:
      - uses: actions/checkout@v6
      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          working-directory: crates/box-intersect-lib-py
          python-version: '3'
      - name: Set up Python
        run: uv python install
      - name: Build wheels
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.platform.target }}
          args: --release --out dist --find-interpreter --manifest-path crates/box-intersect-lib-py/Cargo.toml
          sccache: ${{ !startsWith(github.ref, 'refs/tags/') }}
      - name: Upload wheels
        uses: actions/upload-artifact@v5
        with:
          name: wheels-macos-${{ matrix.platform.target }}
          path: dist
      - name: pytest
        run: |
          set -ex
          cd crates/box-intersect-lib-py
          uv sync --no-install-project --no-build
          ls -l ../../dist
          cd ../..
          uv pip install box_intersect_lib_py --find-links dist --force-reinstall --no-index --python crates/box-intersect-lib-py/.venv/bin/python
          cd crates/box-intersect-lib-py
          uv run --no-sync pytest

  sdist:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Build sdist
        uses: PyO3/maturin-action@v1
        with:
          command: sdist
          args: --out dist --manifest-path crates/box-intersect-lib-py/Cargo.toml
      - name: Upload sdist
        uses: actions/upload-artifact@v5
        with:
          name: wheels-sdist
          path: dist

  release:
    name: Release
    runs-on: ubuntu-latest
    if: ${{ startsWith(github.ref, 'refs/tags/') || github.event_name == 'workflow_dispatch' }}
    needs: [linux, musllinux, windows, macos, sdist]
    permissions:
      # Use to sign the release artifacts
      id-token: write
      # Used to upload release artifacts
      contents: write
      # Used to generate artifact attestation
      attestations: write
    steps:
      - uses: actions/download-artifact@v6
      - name: Generate artifact attestation
        uses: actions/attest-build-provenance@v3
        with:
          subject-path: 'wheels-*/*'
      - name: Install uv
        if: ${{ startsWith(github.ref, 'refs/tags/') }}
        uses: astral-sh/setup-uv@v7
      - name: Publish to PyPI
        if: ${{ startsWith(github.ref, 'refs/tags/') }}
        run: uv publish 'wheels-*/*'
        env:
          UV_PUBLISH_TOKEN: ${{ secrets.PYPI_API_TOKEN }}
